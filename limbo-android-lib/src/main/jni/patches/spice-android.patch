--- a/meson.build
+++ b/meson.build
@@ -104,7 +104,11 @@ foreach dep : ['libjpeg', 'zlib']
   spice_server_deps += dependency(dep)
 endforeach
 
-if host_machine.system() != 'windows'
+if host_machine.system() == 'android'
+  # bionic provides the rt functions in libc; there is no librt on Android
+  foreach dep : ['m']
+    spice_server_deps += compiler.find_library(dep)
+  endforeach
+elif host_machine.system() != 'windows'
   foreach dep : ['rt', 'm']
     spice_server_deps += compiler.find_library(dep)
   endforeach
